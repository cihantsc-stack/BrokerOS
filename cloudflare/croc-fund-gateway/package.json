{
  "name": "croc-fund-gateway",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "check": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
